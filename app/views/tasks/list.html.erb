<h1>Here are the list of all the tasks:</h1>
<ul><% @tasks.each do |task| %>
  <li>
    <%= link_to task.title, task_path(task) %> - <%= task.details %> - <%= task.completed %>
  </li>
  <% end %>
</ul>

<%= link_to "Create a new task", new_path %>
